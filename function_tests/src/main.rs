fn main() {
    println!("damn.");
    new_function(5);
}

fn new_function(x:u64) { 
    println!("isn't code that executes not-top to bottom kinda weird to you, or is that just me?, also here's an variable: {x}");
}
